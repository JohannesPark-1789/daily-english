# daily-english — 월~금 오전 7시, 오늘의 영어 한 문장

카카오톡 **"나에게 보내기"** 로 매일 아침 영어 표현 하나 + 쇼츠 링크를 받는다.
받은 메시지를 단톡방에 전달하는 것은 손으로 한다 (§4 — 왜 자동이 아닌지).

> 최종 갱신: 2026-08-22 · 스캐폴딩 완료, 발송 경로는 인증(§3) 후 동작

---

## 1. 무엇을 하나

```
data\phrases.tsv (235문장)      config.json (템플릿·요일·시각)
            └──────┬──────────────────┘
        작업 스케줄러 (월~금 07:00)
                   ↓
        scripts\send-today.ps1
                   ↓  진행상태 state\progress.json 에서 다음 번호를 집는다
     카카오톡 "나에게 보내기" API → 나와의 채팅
```

- 하루 한 문장, **순서대로** 나간다. 235문장이면 주 5일 기준 **47주치**.
- 같은 날 두 번 실행돼도 두 번 보내지 않는다 (`lastSentDate` 검사).
- PC 가 꺼져 있어 놓친 날은 `-StartWhenAvailable` 로 켠 뒤 한 번 따라 실행된다.
  날짜를 건너뛰어도 번호는 밀리지 않는다 (날짜 기준이 아니라 진행 번호 기준).
- 목록을 다 쓰면 멈춘다. `config.json` 의 `onFinish: "loop"` 로 바꾸면 처음부터 다시 돈다.

### 주말 복습 카드 (토 07:00)

평일에 받은 문장을 토요일 아침에 카드로 다시 본다. 카톡은 링크만 나르고, 복습은 웹페이지가 한다.

```
토 07:00  send-weekly.ps1 → 그 주 로그에서 발송분을 뽑아 문장 목록 + 카드 링크 발송
                              ↓
   web\index.html  한글↔영어 방향 선택 · 3단계 자기평가 · 못 맞힌 것만 다시
                   "누적 연습" 탭은 지금까지 받은 전체에서 오답 위주로 20장
```

- 카드 범위: 링크의 `?w=1,4,7` 파라미터 → 없으면 시작일·평일·`skipNos` 로 역산 → 누적은 localStorage
- 복습 기록은 **폰 안에만** 남는다 (서버 없음). 기기가 바뀌면 기록도 따라오지 않는다
- 문장·번역을 고치면 `build-web.ps1` 을 다시 돌리고 호스팅에 올린다
- 설계 배경과 호스팅 선택지는 [docs/REVIEW_CARDS.md](docs/REVIEW_CARDS.md)

### 번역 검수

카드 뒷면(한글)은 기계적으로 옮긴 초안이다. Excel 로 왕복하며 고친다.

```
review-export.cmd   →  data\review.csv 를 만들고 Excel 로 연다
                       번호 / 영어 / 한글 / 확신도 / 검수메모 / 쇼츠링크가 한 줄에 있다
                       "한글" 열만 고치고 저장 (CSV 형식 유지)
review-import.cmd   →  바뀐 줄만 반영 (전/후를 보여준다) + 백업 + 카드 다시 빌드
git push            →  사이트 반영
```

- **`확신도 = 확인필요` 인 51개부터** 보면 된다. 문맥·말투에 따라 뜻이 갈려 옮기기 어려웠던 것들이고,
  왜 어려웠는지는 `검수메모` 열에 있다. `review-export.cmd -OnlyCheck` 로 그것만 뽑을 수 있다
- 각 줄의 **쇼츠 링크를 클릭하면** 그 표현이 쓰인 장면을 바로 확인할 수 있다
- 고친 줄은 `검수완료` 로 표시된다. 빈 칸으로 둔 줄은 무시한다 (실수로 지운 경우 보호)
- 반영 전 `data\backup\translations-<시각>.tsv` 로 백업된다. `-WhatIfOnly` 로 미리 볼 수도 있다

## 2. 실행

```
C:\Dev\daily-english\
├── setup.cmd            ← 최초 1회 카카오 인증
├── preview.cmd          ← 발송 안 하고 내용만 확인 (-No 42 로 특정 번호)
├── send-now.cmd         ← 지금 즉시 발송 (요일·중복 검사 무시)
├── register-task.cmd    ← 월~금 07:00 등록 / -Weekly 토요일 / -Remove / -Status
├── config.json          ← 메시지 템플릿·발송 요일·시각·버튼 문구·카드 주소
├── .env                 ← 카카오 REST API 키·시크릿 (커밋 금지)
├── data\
│   ├── phrases.tsv      ← 번호 / 표현 / 쇼츠 링크 (탭 구분)
│   └── translations.tsv ← 번호 / 한글 뜻 — 카드 뒷면
├── web\
│   ├── template.html    ← 카드 앱 원본 (데이터 자리표시자 포함)
│   ├── index.html       ← 빌드 결과 · 단독 호스팅용 (완전한 문서)
│   └── artifact.html    ← 빌드 결과 · Artifact 발행용 (본문만)
├── state\               ← tokens.json · progress.json (커밋 금지)
├── logs\                ← daily-english-YYYY-MM.log
└── scripts\
    ├── setup-auth.ps1   ← 인증 코드 → 토큰
    ├── send-today.ps1   ← 평일 발송 (-DryRun / -No / -Force)
    ├── send-weekly.ps1  ← 주말 복습 발송 (-DryRun / -Date / -Force)
    ├── build-web.ps1    ← TSV → 카드 앱 빌드
    ├── register-task.ps1
    └── lib\kakao.psm1   ← 설정·토큰·발송·목록·진행상태
```

## 3. 최초 설정 (한 번만)

1. [카카오 디벨로퍼스](https://developers.kakao.com) → 애플리케이션 추가
2. **카카오 로그인** 활성화 → **Redirect URI** 에 `http://localhost:8910/callback` 등록
3. **동의항목** → `카카오톡 메시지 전송(talk_message)` 을 선택 동의로 설정
4. `.env.example` 을 `.env` 로 복사하고 **REST API 키**를 붙여넣는다
5. `setup.cmd` 실행 → 브라우저에서 로그인·동의 → 주소창 URL 을 붙여넣기
6. `register-task.cmd` 실행 → 월~금 07:00 등록

비밀번호는 **브라우저(카카오 로그인 화면)에만** 입력한다. 스크립트는 비밀번호를 받지 않고,
받는 것은 리다이렉트 주소에 실려 오는 인증 코드뿐이다.

### 토큰 관리

- access token 은 만료 10분 전에 자동 갱신된다 (refresh token 사용).
- **refresh token 은 유효기간이 2개월**이다. 매일 발송이 돌면 자동으로 계속 갱신되지만,
  두 달 넘게 한 번도 안 돌면 만료된다 → `setup.cmd` 를 다시 실행하면 된다.

## 4. 왜 단톡방 자동 발송이 아닌가

카카오 정책 때문이다. 확인한 내용:

| 경로 | 가능? | 이유 |
|---|---|---|
| 나에게 보내기 | ✅ | 무료, 승인 불필요. **본인에게만** |
| 친구에게 보내기 API | ❌ | 비즈앱(사업자등록증) + 권한 검수 필요. 카카오가 **"자동 메시지는 지양, 검수 반려 가능"** 으로 명시. 수신자마다 앱 로그인·동의도 필요 |
| 알림톡 / 친구톡 | △ | 정식 경로. 사업자등록증 + 채널 + 템플릿 심사, 건당 8~13원. 친구톡은 수신자가 채널을 추가해야 함 |
| PC 카톡 UI 자동화 | ⛔ | 약관 위반, 계정 정지 위험. 쓰지 않는다 |

여러 명에게 자동으로 보내야 한다면 **카카오톡 채널 + 친구톡**으로 가야 한다.
그 경우 `send-today.ps1` 의 발송 함수만 비즈메시지 API 로 바꾸면 나머지(스케줄·진행상태·템플릿)는 그대로 쓴다.

## 5. 주의

- `.env` · `state\tokens.json` 은 **커밋 금지**. `.gitignore` 에 들어 있다.
- `.ps1` / `.psm1` 은 **UTF-8 with BOM + CRLF**. BOM 없으면 PS 5.1 이 한글을 깨뜨린다.
- 텍스트 템플릿은 **200자 제한**. 넘으면 잘린다 (`Send-DeKakaoMemo` 에서 자동 절단).
- 문장 목록은 `data\phrases.tsv` 하나만 고치면 된다 (탭 구분: 번호 / 표현 / 링크).
  원본은 `mp423` 에서 mp3 로 뽑은 것과 같은 목록이다.
- `@($list)` 로 `List[object]` 를 감싸면 PS 5.1 에서 터진다 (`ToArray()` 를 쓴다). 실제로 겪었다.
