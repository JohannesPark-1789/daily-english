# 비밀값·토큰 — 절대 커밋 금지
.env
state/tokens.json
state/progress.json

# 로그
logs/*
!logs/.gitkeep
state/*
!state/.gitkeep

# 잡파일
Thumbs.db
desktop.ini

# 번역 검수 작업 파일 (왕복용, 추적하지 않음)
data/review*.csv
data/backup/
